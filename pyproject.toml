[project]
name = "letterboxd-2-radarr"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "arrapi>=1.4.14",
    "bs4>=0.0.2",
    "requests>=2.34.2",
    "rich>=15.0.0",
    "typer>=0.26.7",
]

[project.scripts]
letterboxd-2-radarr = "app.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["app"]
